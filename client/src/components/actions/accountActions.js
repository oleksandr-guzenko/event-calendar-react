import axios from 'axios';

import {
    GET_ACCOUNTS,
    GET_ACCOUNT,
    ADD_ACCOUNT,
    UPDATE_ACCOUNT,
    DISABLE_ACCOUNT
} from './types.js';

// get all accounts you can access 
export const getAccounts = dispatch => {

}

// get the account to amend
export const getAccount = dispatch => {
    
}

// create a new account
export const addAccount = dispatch => {}

// amend an account
export const updateAccount = dispatch => {}

// disable an account
export const disableAccount = dispatch => {}